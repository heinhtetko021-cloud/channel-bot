name: htech-daily-drafts
on:
  workflow_dispatch:
  schedule:
    - cron: "30 2 * * *"
    - cron: "30 11 * * *"
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - run: curl -s -o NUL -w "HTTP %{http_code}" -X POST -H "Authorization: Bearer test" https://channel-bot-jade.vercel.app/api/cron